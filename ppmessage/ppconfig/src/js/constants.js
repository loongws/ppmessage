// the [] is must, otherwise it is a ref not define
angular.module("this_app.constants", [])
    .constant("yvConstants", {
        
        TEMPLATE_PREFIX: "templates/",
        STATIC_PREFIX: "/ppconfig/static/",
        
    });
